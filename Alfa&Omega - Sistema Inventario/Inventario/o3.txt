70:        tk.Label(filters, text="Estado:", bg=_BG_DARK, fg="white").grid(row=0, column=2, padx=4, pady=2, sticky="e")
71:        self.var_status_filter = tk.StringVar()
72:        self.cb_status_filter = ttk.Combobox(filters, textvariable=self.var_status_filter,
74:        self.cb_status_filter.grid(row=0, column=3, padx=4, pady=2)
75:        self.cb_status_filter.bind("<<ComboboxSelected>>", lambda e: self._load_orders())
99:        columns = ("numero", "supplier", "fecha", "total", "estado")
101:        for col, label in zip(columns, ["Número", "Proveedor", "Fecha", "Total", "Estado"]):
109:        status = self.var_status_filter.get() or None
110:        orders = list_purchase_orders(supplier_code=supplier, estado=status)
119:                order["estado"]
124:        self.var_status_filter.set("")
204:            detail += f"Estado: {self.selected_order['estado']}\n"
415:        info += f"Estado: {self.selected_order['estado']}"